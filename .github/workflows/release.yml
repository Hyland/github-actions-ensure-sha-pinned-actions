name: Release

on:
  pull_request:
    types:
      - closed
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  release:
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
    uses: Alfresco/alfresco-build-tools/.github/workflows/reusable-release.yml@e87345fb177248fc5bdc7838107f098438818df1 # v18.21.0
    with:
      release_type_override: ${{ inputs.release_type }}
    secrets:
      BOT_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
